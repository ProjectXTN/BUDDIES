import React from 'react';
import ReactDOM from 'react-dom/client';
import SearchBar from './searchBar';

//recherche de la div avec l'ID root
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <SearchBar></SearchBar>
)

